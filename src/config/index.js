const config = {
    API_BASE_URL: process.env.REACT_APP_API_BASE_URL || "http://localhost:4000/api",
    VERSION_CODE: process.env.REACT_APP_VERSION_CODE || "300",
    CURRENT_VERSION: process.env.REACT_APP_CURRENT_VERSION || "2.0.8",
};

export default config;
